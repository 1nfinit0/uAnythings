	Cambio de nombre a un Switch:
enable			entra en modo exec privilegiado, sin ello es solo exec
conf terminal		
hostname NOMBRE
end

	Setear una contraseña
enable
conf terminal
line console 0
password contraseña
login  (habilita la contraseña para exec)
end
exit
configure terminal
enable secret password (habilita la contraseña para exec provilegiado)
exit
exit

	
